{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uv run uvicorn src.backend.main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
